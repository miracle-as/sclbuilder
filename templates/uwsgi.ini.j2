# mysite_uwsgi.ini file
[uwsgi]
chdir           = /opt/{{ scluirepository }}/{{ project }}
module          = {{ django_app }}.wsgi
home            = /opt/{{ scluirepository }}/venv
master          = true
processes       = 10
socket          = /opt/socket/{{ project }}.sock
chmod-socket    = 666
vacuum          = true

